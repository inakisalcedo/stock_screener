# Archivo __init__.py del paquete "controlador".
# Se mantiene vacío intencionalmente; su única función es indicarle a Python
# que la carpeta "controlador" debe tratarse como un paquete importable,
# permitiendo hacer `from controlador import control` desde app.py.
